{% extends 'base.html.twig' %}

{% block body %}

            <div class="container">
                {{ form_start(user) }}

                <div class="form-group col-6 col-md-5 ms-auto me-auto">
                    {{ form_widget(user) }}
                </div>
                <div class="text-center">
                    <button type="submit" class="btn btn-success">Update!</button>
                    {{ form_end(user) }}
                </div>



            </div>


{% endblock %}

{% block footer %}{% endblock %}
